# Cesium React + Tailwind 项目

基于 React + TypeScript + Tailwind CSS + Cesium 的 3D 地图可视化项目。

## 功能特性

- 🗺️ **3D 地图展示**：基于 Cesium 的高性能 3D 地球展示
- 🎨 **现代化 UI**：使用 Tailwind CSS 构建的美观界面
- 📍 **多种标注工具**：支持点、线、箭头等多种地图标注
- 🚀 **动画演示**：支持 3D 模型动画和路径动画
- 🏛️ **中国边境线**：自动加载并显示中国各省份边境线
- 🎯 **位置跳转**：快速跳转到预设地理位置
- 🖼️ **图标绘制**：支持在地图上绘制自定义图标
- ⚙️ **可拖拽面板**：可拖拽的操作面板，支持展开/收起

## 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Cesium** - 3D 地图引擎
- **React Router** - 路由管理

## 项目结构

```
project_cesium_react/
├── public/                 # 静态资源
│   ├── GeoData.json       # 地理数据
│   ├── images/            # 图片资源
│   └── models/            # 3D 模型
├── src/
│   ├── components/        # 通用组件
│   │   ├── ButtonComponent.tsx
│   │   └── TableComponent.tsx
│   ├── views/             # 页面组件
│   │   └── CesiumContainer/
│   │       ├── CesiumMap.tsx          # 主地图组件
│   │       ├── CesiumController.ts    # Cesium 控制器
│   │       ├── CesiumUtils.ts         # Cesium 工具函数
│   │       ├── const.ts               # 常量定义
│   │       └── components/            # 子组件
│   │           ├── ConfigContainer.tsx
│   │           ├── DrawAnimationContainer.tsx
│   │           ├── DrawFlagContainer.tsx
│   │           ├── LocationJumpContainer.tsx
│   │           └── MarkContainer.tsx
│   ├── App.tsx            # 根组件
│   ├── main.tsx           # 入口文件
│   └── index.css          # 全局样式
├── vite.config.js         # Vite 配置
├── tailwind.config.js     # Tailwind 配置
└── tsconfig.json          # TypeScript 配置
```

## 快速开始

### 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 启动开发服务器

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

### 构建生产版本

```bash
npm run build
# 或
yarn build
# 或
pnpm build
```

## 主要功能说明

### 1. 标注模式

提供多种地图标注工具：
- **点**：在地图上绘制点
- **线段**：绘制线段
- **直线箭头**：绘制直线箭头
- **攻击箭头**：绘制攻击箭头
- **钳形箭头**：绘制钳形箭头

### 2. 图标绘制

支持在地图上绘制自定义图标（如红旗、绿旗等）。

### 3. 动效演示

支持播放预设的动画脚本，包括：
- 3D 模型飞行路径
- 多路径动画
- 自定义动画数据

### 4. 位置跳转

快速跳转到预设位置：
- 北京
- 台湾（高雄/台南）

### 5. 通用配置

- 显示/隐藏中国边境线
- 其他配置选项

## 开发说明

### Cesium 配置

项目使用 `vite-plugin-cesium` 插件来集成 Cesium。Cesium 的静态资源会自动处理。

### 样式

项目使用 Tailwind CSS 进行样式管理。所有组件都使用 Tailwind 工具类进行样式设置。

### 地图控制器

`CesiumController` 类提供了所有 Cesium 相关操作的统一接口：
- `init_world()`: 初始化 Cesium 场景
- `mark()`: 添加标注
- `drawChinaBorder()`: 绘制中国边境线
- `flyToLocation()`: 跳转到指定位置
- `showSituation()`: 显示动画演示

## 注意事项

1. **Cesium Token**：如果需要使用 Cesium Ion 的默认底图，需要配置 Access Token（当前使用第三方底图服务）
2. **静态资源**：确保 `public` 目录下的 GeoData.json 和模型文件存在
3. **浏览器兼容性**：Cesium 需要支持 WebGL 的现代浏览器

## 许可证

MIT

## 贡献

欢迎提交 Issue 和 Pull Request！